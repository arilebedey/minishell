#include "../../include/command.h"
#include "../../include/env.h"

int	parse(const char* line, t_command *head_cmd, t_env *head_env)
{
	//TODO : Lexer->Checker->Assembler
	if (head_cmd || line || head_env)
	{

	}
	return (1);
}